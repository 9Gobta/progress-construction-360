# Shared Python

Code stays inside the modular API package during the MVP. Move a module here only when it is reused by independently deployable processes and has its own tests and version boundary.

