# Machine learning workspace

- `datasets/`: versioned manifests and label schemas, never raw videos
- `experiments/`: reproducible experiment configuration and reports
- `models/`: model cards and artifact metadata, never untracked binary weights

Raw construction data remains under `Data/` and is excluded from Git.

